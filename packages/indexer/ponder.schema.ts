/**
 * Ponder schema — mirrors the API's PostgreSQL schema.
 * Ponder writes to the same database as the API.
 */
export {};
// In production: define Ponder entity schemas matching packages/api/src/db/schema.ts
// For now, the indexer shares the Drizzle schema from the API package.
